language: r
sudo: false
cache: packages

r_binary_packages:
  - devtools
  - ggplot2
  - testthat
  - xml2
  - doParallel
  - NMF
  - igraph
  - knitr
  - XML
  - covr
  - lintr
  - dplyr
  - lubridate

notifications:
    email:
      on_success: change
      on_failure: change

after_success:
  - Rscript -e 'library(covr); codecov()'

env:
- LINTR_COMMENT_BOT=true
